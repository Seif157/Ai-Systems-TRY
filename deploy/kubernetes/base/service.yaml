apiVersion: v1
kind: Service
metadata:
  name: erp-ai
  namespace: erp-ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: erp-ai
  ports:
    - {name: http, port: 8080, targetPort: http, protocol: TCP}
